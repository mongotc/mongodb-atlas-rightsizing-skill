{
  "_comment": "Targets file for: python rightsizing.py --config config.example.json --out-dir ./report. Omit 'cluster' to audit every cluster in that project. CLI --days/--hours override the values here. Credentials come from environment variables (see .env.example), never from this file.",
  "days": 7,
  "targets": [
    { "group_id": "5f8a1b2c3d4e5f6a7b8c9d0e", "cluster": "prod-cluster-0" },
    { "group_id": "5f8a1b2c3d4e5f6a7b8c9d0e", "cluster": "prod-cluster-1" },
    { "group_id": "6a1b2c3d4e5f6a7b8c9d0e1f" }
  ]
}
